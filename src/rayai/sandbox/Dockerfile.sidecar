FROM python:3.12-slim

# Install minimal dependencies
RUN pip install --no-cache-dir requests

# Create directory for socket
RUN mkdir -p /tmp

# Copy proxy server code
COPY mcp_proxy.py /app/mcp_proxy.py

WORKDIR /app

# Run proxy server
CMD ["python", "mcp_proxy.py"]
